{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "Agenticana/memory-schema",
  "title": "Agenticana Soul Memory Entry",
  "description": "Schema for persistent cross-session key-value memory entries",
  "type": "object",
  "required": ["key", "value", "tags", "score", "created_at"],
  "properties": {
    "key": {
      "type": "string",
      "description": "Unique identifier for the memory entry (kebab-case)"
    },
    "value": {
      "type": "string",
      "description": "The memory content — plain text or structured summary"
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Tags for categorisation and search"
    },
    "score": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Relevance score (0–1) for ranking during search"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 creation timestamp"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 last-update timestamp"
    },
    "access_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of times this entry has been retrieved"
    }
  }
}
